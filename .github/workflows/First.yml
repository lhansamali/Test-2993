name: first
on:
  pull_request:
    types: [opened]
jobs:
  job1:
    name: First_job
    runs-on: ubuntu-latest
    steps:   
      - name: step one
        uses: kentaro-m/auto-assign-action@v2.2.0
        with:
          reviewers: |    
            - SongRaein

      - name: step three
        run: echo "Added Reviewer"
